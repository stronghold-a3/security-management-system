/**\n * Route lazy loading configuration\n * Implements code splitting for routes to reduce initial bundle size\n *\n * What it does:\n * - Lazy loads route components with React.lazy()\n * - Provides loading fallback UI\n * - Implements Suspense boundary for error handling\n * - Improves page load performance\n */\n\nimport { lazy, Suspense, ComponentType, ReactNode } from 'react';\nimport { Loader } from 'lucide-react';\n\n/**\n * Loading fallback component\n */\nexport const LoadingFallback = () => (\n  <div className=\"flex items-center justify-center min-h-screen\">\n    <div className=\"text-center\">\n      <Loader className=\"w-8 h-8 animate-spin mx-auto mb-4 text-blue-600\" />\n      <p className=\"text-gray-600\">Loading...</p>\n    </div>\n  </div>\n);\n\n/**\n * Higher-order component for lazy loading routes\n */\nexport const lazyRoute = (\n  importFunc: () => Promise<{ default: ComponentType<any> }>,\n  fallback?: ReactNode\n): ComponentType => {\n  const Component = lazy(importFunc);\n\n  return (props: any) => (\n    <Suspense fallback={fallback || <LoadingFallback />}>\n      <Component {...props} />\n    </Suspense>\n  );\n};\n\n/**\n * Dynamic import with error handling\n */\nexport const dynamicImport = async <T extends Record<string, any>>(\n  importFunc: () => Promise<T>\n): Promise<T> => {\n  try {\n    return await importFunc();\n  } catch (error) {\n    console.error('Failed to dynamically import module', error);\n    throw error;\n  }\n};\n\n/**\n * Example route configurations with code splitting\n * Use these as a template for your routes\n */\nexport const lazyRoutes = {\n  // Dashboard\n  Dashboard: lazyRoute(() => import('../pages/Dashboard')),\n  \n  // Security\n  SecurityOverview: lazyRoute(() => import('../pages/SecurityOverview')),\n  SecuritySettings: lazyRoute(() => import('../pages/SecuritySettings')),\n  \n  // Users\n  UserManagement: lazyRoute(() => import('../pages/UserManagement')),\n  UserDetails: lazyRoute(() => import('../pages/UserDetails')),\n  \n  // Reports\n  Reports: lazyRoute(() => import('../pages/Reports')),\n  ReportDetails: lazyRoute(() => import('../pages/ReportDetails')),\n  \n  // Settings\n  Settings: lazyRoute(() => import('../pages/Settings')),\n  \n  // NotFound\n  NotFound: lazyRoute(() => import('../pages/NotFound')),\n};\n\nexport default lazyRoute;\n
